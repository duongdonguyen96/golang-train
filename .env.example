# App
APP_ADDR=":8080"

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root

# Multi-schema (shared + per-tenant)
DB_PUBLIC_SCHEMA=public
DB_TENANT_SCHEMA_PREFIX=tenant_

# Goose migrations
MIGRATIONS_DIR=migrations
